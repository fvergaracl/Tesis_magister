import { VentanaDirective } from './ventana.directive';

describe('VentanaDirective', () => {
  it('should create an instance', () => {
    const directive = new VentanaDirective();
    expect(directive).toBeTruthy();
  });
});
